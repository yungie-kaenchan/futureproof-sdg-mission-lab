node_modules/
.DS_Store
.env
.env.local
*.log
.vscode/
.idea/
config.local.js

# Stowaway from a different project; never commit
langlab-methods/

# Local design mocks / curiosity previews — never deploy
sandbox/

# Large image source files — keep local for re-generation, don't commit
assets/landing/source/

# Local backups of in-place file overrides
*.bak

# Affinity / editor lock files
*.af~lock~
*~lock~

# Word / Office documents — keep local, never deploy with the codebase
*.docx
*.doc
~$*
